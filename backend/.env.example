# backend/.env.example — 복사해서 backend/.env 로 저장 후 값 수정
# ========== 로컬 개발 ==========
DATABASE_URL=sqlite:///./playmolt.db
REDIS_URL=redis://localhost:6379

# ========== 서버 배포 (오라클 서버 등 Linux) ==========
# PostgreSQL 권장. 같은 서버에 DB 설치 시 예:
# DATABASE_URL=postgresql://playmolt:비밀번호@localhost:5432/playmolt
# 원격 DB 사용 시:
# DATABASE_URL=postgresql://user:pass@dbhost:5432/playmolt
# REDIS_URL=redis://localhost:6379
# (서버에서는 DATABASE_URL만 서버용으로 바꾸면 됨. Windows가 아니면 PostgreSQL 그대로 사용)

JWT_SECRET=dev-secret-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

API_KEY_PREFIX=pl_live_
ALLOWED_ORIGINS=http://localhost:3000
ABANDONED_GAME_MINUTES=30

# 구글 로그인 (선택). Google Cloud 콘솔에서 OAuth 2.0 클라이언트 ID(웹 앱) 생성 후
# GOOGLE_CLIENT_ID=585929805891-3q656bu3rgscd91ife7pgj5dnhsekcvk.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=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
# 리디렉트 URI: http://localhost:8000/api/auth/google/callback (개발) / 실제 도메인 (배포)
# GOOGLE_REDIRECT_URI=http://localhost:8000/api/auth/google/callback
# GOOGLE_AUTH_SUCCESS_REDIRECT=http://localhost:3000

# 서버 배포 시 권장
# APP_ENV=production
# ALLOWED_ORIGINS=https://your-domain.com

# 관리자 API용 (선택)
# ADMIN_SECRET=서버용비밀값
