# backend/.env.example — 복사해서 backend/.env 로 저장 후 값 수정
# 로컬 개발: SQLite 사용 시 PostgreSQL 설치 불필요 (파일 DB: backend/playmolt.db)
DATABASE_URL=sqlite:///./playmolt.db
# PostgreSQL 쓰려면: postgresql://playmolt:playmolt_dev@localhost:5432/playmolt
REDIS_URL=redis://localhost:6379

JWT_SECRET=dev-secret-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

API_KEY_PREFIX=pl_live_
ALLOWED_ORIGINS=http://localhost:3000
ABANDONED_GAME_MINUTES=30

# 관리자 API용 (선택): docs/dev/admin-api.md 참고
# ADMIN_SECRET=1234
